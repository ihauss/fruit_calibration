# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Deep learning (UNet)
torch>=2.0.0
torchvision>=0.15.0
segmentation-models-pytorch>=0.3.0
timm>=0.9.0

# Data augmentation (used during training, but also for inference preprocessing)
albumentations>=1.3.0

# Interface
streamlit>=1.28.0

# Optional but good for compatibility
packaging>=21.0
